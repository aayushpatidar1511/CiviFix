# AI / ML

The current baseline implements deterministic, explainable civic priority scoring and PostGIS + token-set text similarity for duplicate candidates. `AIPrediction` and `RiskScore` are persisted model-result entities for subsequent evaluated scikit-learn inference. Predictions must be treated as recommendations, not authoritative decisions.
